import 'package:flutter/material.dart';

class AfterRent extends StatelessWidget {
  const AfterRent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("HI AFTER"),
    );
  }
}
