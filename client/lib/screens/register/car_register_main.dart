import 'package:flutter/material.dart';


class CarRegister extends StatelessWidget {
  const CarRegister({Key? key}) : super(key: key);

  @override

  Widget build(BuildContext context) {
    return MaterialApp(
      theme: ThemeData(),
      home: const Scaffold(
        backgroundColor: Colors.white,
        body: Text('hi'),
      ),
    );
  }
}
